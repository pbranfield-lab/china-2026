# Handoff — start here in a fresh session

**Last updated: 2026-08-09 09:00.** Read this, then `CLAUDE.md` for the
architecture. There is only ever **one** handoff file, timestamped in its
filename — `git mv` it to the new timestamp when you update it; if you find
two, the newest wins.

## Where this stands

**v3.1.0 — the flow/interactivity pass on top of the v3.0.0 zine re-skin.**
Paul liked the zine skin ("yes thats better") and asked for flow fixes and
more draw-them-in interactivity. Shipped, all in `CLAUDE.md`'s architecture
notes:

- **Contextual nav** — trip links hidden on the home page (it's the chooser);
  a trip chip appears in the header on every other page, linking back.
- **The Big Quiz** on `story.html` — 5 random questions auto-built from
  `FACTS` (numeric stats only), Maisie-voice feedback, replayable.
- **Home teaser** — random "Did you know?" fact with a 🎲 reshuffle and a
  link into that trip's facts + quiz. Sits *after* the trip chooser.
- **Cover-line counts** on the trip cards ("12 spots · 45 photos · 10 facts").

`node tools/check.mjs` passes all 14 (two new checks: quiz/teaser containers).

**Mobile at 375px was actually verified this time** (Playwright): no
horizontal overflow on any page, nav chip truncates, quiz stacks to one
column, answer flow works. Desktop has **not** been eyeballed since v3.0.0
shipped — that's still the open verification, along with the v3.0.0 list
(home hero stickers, map pins on all three plans, facts popout, William's
speech-bubble tail, reduced motion).

## Known cosmetic nits (deliberate deferrals)

- Quiz question phrasing is the fact `label` verbatim; most read fine but
  "Guess the number: London to New York" (Great Wall, stat "3.8x") is
  cryptic until the answer reveals the explanation. Fix would be an optional
  `q` field per fact — only if Paul wants it.
- The favicon 404s (no favicon ever existed). A red seal-chop favicon would
  suit the zine skin.

## Loose ends — every one needs Paul, not code

1. **Eyeball the live site on desktop** — hard-refresh, `styles.css` caches.
2. **⚠️ Great Wall caption identities were inferred from outfits, not
   confirmed** — table in `docs/great-wall-moments.md`. Live and public.
3. **Source URLs for the three sourced Xi'an photos** to complete TASL
   attribution (`credit` objects render author + licence, lack title + source).
4. **One look at the rendered CC credit lines** on `gallery.html?trip=xian` —
   shipped unverified back at v1.4.1.
5. Great Wall family moments / William quotes — none yet by design; see
   `docs/great-wall-moments.md`.
6. Housekeeping OKs pending: delete superseded `docs/terracotta-plan.md`;
   remove stale worktree `.claude/worktrees/cloisonne-reskin/`;
   `forbidden-city/` has ~20 unreferenced images (harmless); empty
   `Photographs/shanghai/` and `Photographs/xitan/` folders.

**More trips are coming** — the expected next job after verification.
`docs/authoring.md` covers adding one end to end.

## How to verify

```bash
cd C:\Claude\China
python3 -m http.server 8000
node tools/check.mjs      # must print "All checks passed."
```

## Closing out a session

1. Rewrite this file: current state, what's next, open decisions. **Keep it
   under ~5 KB.** Don't duplicate `CLAUDE.md`, `docs/authoring.md` or memory —
   architecture and gotchas belong in `CLAUDE.md`, content/photo workflow in
   `authoring.md`, finished-work narrative in git history.
2. `git mv docs/HANDOFF-<old>.md docs/HANDOFF-YYYY-MM-DD-HHMM.md` (pointers in
   `README.md` and `CLAUDE.md` say "newest", so they don't need touching).
3. Update `CLAUDE.md` if the *architecture* changed.
4. Bump `SITE_VERSION` in `assets/data.js` if content or behaviour changed.
5. Commit and push — pushing `master` publishes. Check `git remote -v` says
   `china-2026` first.
