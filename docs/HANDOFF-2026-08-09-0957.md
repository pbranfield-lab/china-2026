# Handoff — start here in a fresh session

**Last updated: 2026-08-09 09:57.** Read this, then `CLAUDE.md` for the
architecture. There is only ever **one** handoff file, timestamped in its
filename — `git mv` it to the new timestamp when you update it; if you find
two, the newest wins.

## Where this stands

**v3.2.0 — two new trips: Xitang Water Town and Shanghai.** The site now has
five trips. Everything the architecture prescribes was done: data files
(`assets/data/xitang.js`, `shanghai.js`), TRIP_MODULES, script tags on all
five pages, check.mjs DATA_FILES, schematic SVG maps
(`assets/xitang-map.svg`, `shanghai-map.svg`), 10 verified facts each, and
the `maisie` subagent wrote all the copy (every photo was opened first).
`node tools/check.mjs` passes all 14.

Also in v3.2.0: **every quizzable fact now carries a `q` field** — a
plain-English question ("How long is all the Great Wall put together,
compared to the distance from London to New York?") — after Paul flagged the
label-based questions as incomprehensible. The quiz prefers `q`, falls back
to the old phrasing. All five trips have full `q` coverage.

Media notes: the six Xitang shoot photos were copied from their WhatsApp
names to descriptive ones (WA originals left on disk, untracked); the
Shanghai video was re-encoded HEVC→H.264 at 5.8 MB (58 MB original untracked);
six CC photos were pulled from Wikimedia Commons with full credit objects and
source URLs recorded in `docs/authoring.md`.

**Not yet looked at in a browser:** both new trips (maps + pin positions
especially — the pins were placed by arithmetic on the new SVGs), and the
desktop pass of the v3.0/3.1 zine skin is still owed.

## Loose ends — every one needs Paul, not code

1. **Eyeball the new trips live** — especially map pins on the two new SVGs,
   the Xitang hero (hanfu-parasol-bridge), the Shanghai hero crop, and the
   Bund video playing in the modal.
2. **Family moments checklists**: `docs/xitang-moments.md`,
   `docs/shanghai-moments.md` (what was the woven-ball prop? did they ride
   the Maglev? William quotes for both trips — all `william` fields omitted).
3. **⚠️ Great Wall caption identities still unconfirmed**
   (`docs/great-wall-moments.md`).
4. Xi'an TASL source URLs still missing (Xitang/Shanghai ones are recorded).
5. Housekeeping OKs pending: superseded `docs/terracotta-plan.md`; stale
   worktree `.claude/worktrees/cloisonne-reskin/`; ~20 unreferenced
   forbidden-city images.

Resolved 2026-08-09: Paul OK'd deleting the raw originals — the six WhatsApp
shoot photos and the two Shanghai originals (58 MB HEVC video, 1.9 MB photo)
are deleted from disk; the curated web copies are the only versions now. The
earlier history-purge question is treated as moot, since the same shoot
photos are now deliberately published in curated form.

## How to verify

```bash
cd C:\Claude\China
python3 -m http.server 8000     # then ?trip=xitang / ?trip=shanghai everywhere
node tools/check.mjs            # must print "All checks passed."
```

## Closing out a session

1. Rewrite this file: current state, what's next, open decisions. **Keep it
   under ~5 KB.** Don't duplicate `CLAUDE.md`, `docs/authoring.md` or memory —
   architecture and gotchas belong in `CLAUDE.md`, content/photo workflow in
   `authoring.md`, finished-work narrative in git history.
2. `git mv docs/HANDOFF-<old>.md docs/HANDOFF-YYYY-MM-DD-HHMM.md` (pointers in
   `README.md` and `CLAUDE.md` say "newest", so they don't need touching).
3. Update `CLAUDE.md` if the *architecture* changed.
4. Bump `SITE_VERSION` in `assets/data.js` if content or behaviour changed.
5. Commit and push — pushing `master` publishes. Check `git remote -v` says
   `china-2026` first.
