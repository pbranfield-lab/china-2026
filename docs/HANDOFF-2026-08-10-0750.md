# Handoff — start here in a fresh session

**Last updated: 2026-08-10 07:50.** (The previous handoff was stamped "10:15"
but actually committed 07:17 — this one is newer despite the smaller number.)
Read this, then `CLAUDE.md` for the architecture. There is only ever **one**
handoff file, timestamped in its filename — `git mv` it to the new timestamp
when you update it; if you find two, the newest wins.

## Where this stands

**v4.3.0 — the navigation audit ran and its first fix shipped.** The audit
found: the trip chip lied on trip-agnostic pages (claimed "The Forbidden
City" with no `?trip=`), the link-rewrite pass stamped that silent default
onto every link from those pages, journey's "Games Bit" leg-link carried it
too, map.html is a dead end (no next-cards, no hero, never names its trip),
gallery/family still say "the whole day" (single-day FC-era copy), and the
chip's "⇄" is unexplained on touch.

Paul green-lit **only the truthful-context fix (A)**; the rest are queued
below. What shipped in v4.3.0 (all JS — no HTML/nav-markup changes):

- `store` moved above the trip resolution in site.js; new documented store
  key **`trip`** remembers the last *explicitly* chosen trip.
- `CURRENT_TRIP` resolves explicit `?trip=` → remembered trip → `TRIPS[0]`,
  with new **`TRIP_CHOSEN`** flag (explicit or remembered). Only the
  contextual nav, the rewrite pass and the journey leg-link consult it —
  content code is untouched, so bare old deep links still render `TRIPS[0]`.
- Agnostic pages (family/journey/play) with no chosen trip: trip-scoped nav
  links hide (same as index), the chip becomes "🧭 Pick a trip →", the
  rewrite pass stamps nothing and reroutes trip-scoped links (the bottom
  next-cards) to index.html. Once a trip is chosen anywhere, context follows
  the reader across bare links.
- CLAUDE.md updated (trip resolution, store keys, contextual-nav bullet).
- check.mjs all green; `?v=` restamped to 4.3.0 on all seven pages.

**Not yet browser-verified** — Paul's rule is ask before Playwright. Worth a
manual pass: fresh profile → index → Journey (expect "Pick a trip" chip,
hidden Story/Facts/Map/Gallery links) → a leg-link (expect chip to name that
trip and stick on Play/Family afterwards).

## Queued from the audit (Paul saw these, chose A first)

1. **B — "Next stop →" itinerary flow**: JOURNEY-driven card on each trip's
   story/map/gallery ("Next stop: Xi'an 🚄 1,400 km →", "stop 2 of 5").
2. **C — stamp toasts**: first visit to a trip should toast "New passport
   stamp (3 of 5)" with `SoundKit.fx("stamp")` — stamping is silent today.
3. **E — map dead end**: bottom next-cards + visible trip naming on
   map.html; fix stale "whole day" copy (gallery.html sub, family.html card).
4. **D/F — quest-log "keep exploring" strip** from store data; nav regroup
   (trip cluster together); clearer switch affordance than "⇄".

## Other loose ends

1. Real-device pass still owed: sound toggle on iPhone, hanzi tracing feel,
   zh voice availability. Sound on agnostic no-context pages still plays the
   FC ambience (known, deemed out of scope for A).
2. Carried over: 9 unreferenced FC photos, `docs/*-moments.md` checklists,
   Great Wall caption identities unconfirmed, Xi'an TASL source URLs lost.
3. Bumping `SITE_VERSION` requires re-stamping `?v=` on every page (one
   sed; check.mjs fails loudly if forgotten).

## How to verify

```bash
cd C:\Claude\China
python3 -m http.server 8000
node tools/check.mjs           # must print "All checks passed."
node tools/orphans.mjs         # advisory; 9 known FC leftovers only
```

## Closing out a session

1. Rewrite this file: current state, what's next, open decisions. **Keep it
   under ~5 KB.** Don't duplicate `CLAUDE.md`, `docs/authoring.md` or memory.
2. `git mv docs/HANDOFF-<old>.md docs/HANDOFF-YYYY-MM-DD-HHMM.md`.
3. Update `CLAUDE.md` if the *architecture* changed.
4. Bump `SITE_VERSION` in `assets/data.js` if content or behaviour changed —
   and re-stamp the `?v=` asset links to match.
5. Commit and push — pushing `master` publishes. Check `git remote -v` says
   `china-2026` first.
