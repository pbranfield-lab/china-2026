# Handoff — start here in a fresh session

**Last updated: 2026-08-09 15:52.** Read this, then `CLAUDE.md` for the
architecture. There is only ever **one** handoff file, timestamped in its
filename — `git mv` it to the new timestamp when you update it; if you find
two, the newest wins.

## Where this stands

**v3.2.1 — repo audit and housekeeping pass** on top of v3.2.0 (five trips,
full `q` quiz coverage). Paul asked for a fragmentation audit; the verdict was
that the JS/CSS/data architecture is sound — the debt was at the repo level,
and this pass cleared it:

- **Deleted 10 byte-identical duplicate photos** (`PHOTO-2026-08-06-*`,
  `IMG-20260804-WA0002`) — every one was a raw-named twin of a kept file,
  verified by hash before removal. Zero image data lost.
- **Dropped Noto Sans SC** from all five font links: it was downloaded on
  every page (3 weights) but only ever referenced by an unused CSS token.
  Also removed dead CSS: `--font-hanzi-sans`, `--fret-bright`, `.hero .kicker`.
- **README refreshed** — it still described a 3-trip site with a single
  `data.js` and "adding a trip is pure data"; now matches reality.
- **Docs reorganised**: `Guide/guide.txt` → `docs/forbidden-city-guide-notes.txt`
  (authoring.md updated); cloisonné reskin design/plan →
  `docs/archive/` (CLAUDE.md updated); superseded `docs/terracotta-plan.md`
  deleted; stale empty `.claude/worktrees/cloisonne-reskin/` removed.
- **New advisory tool `tools/orphans.mjs`** — lists unreferenced media,
  counting data-layer, hard-coded-HTML *and* CSS references (family.html
  embeds `maisie-princess-xitang.jpg` directly; a data-only scan wrongly
  flags it).

`node tools/check.mjs` passes all 14. **Nothing is committed** — the working
tree holds the whole pass for Paul to review.

## Loose ends

1. **9 unreferenced photos remain** in `Photographs/forbidden-city/` (run
   `node tools/orphans.mjs`): seven descriptively-named curated candidates
   (`dad-maisie-hug`, `family-trio-gate`, `garden-gate`, two `hill-pavilion-*`,
   `entrance-hall-wide`, `marble-ramp-dragon`) plus raw-named `3.jpg`/`37.jpg`.
   They are published by Pages but shown nowhere — Paul to decide per file:
   wire into `photos` data, or delete.
2. **Eyeball the new trips live** (unchanged from v3.2.0) — map pins on the
   two new SVGs, the Xitang hero, the Shanghai hero crop, the Bund video in
   the modal. The desktop pass of the zine skin is still owed.
3. **Family moments checklists**: `docs/xitang-moments.md`,
   `docs/shanghai-moments.md`; **⚠️ Great Wall caption identities still
   unconfirmed** (`docs/great-wall-moments.md`).
4. Xi'an TASL source URLs still missing (Xitang/Shanghai ones are recorded).

## How to verify

```bash
cd C:\Claude\China
python3 -m http.server 8000     # then ?trip=xitang / ?trip=shanghai everywhere
node tools/check.mjs            # must print "All checks passed."
node tools/orphans.mjs          # advisory: unreferenced media
```

## Closing out a session

1. Rewrite this file: current state, what's next, open decisions. **Keep it
   under ~5 KB.** Don't duplicate `CLAUDE.md`, `docs/authoring.md` or memory —
   architecture and gotchas belong in `CLAUDE.md`, content/photo workflow in
   `authoring.md`, finished-work narrative in git history.
2. `git mv docs/HANDOFF-<old>.md docs/HANDOFF-YYYY-MM-DD-HHMM.md` (pointers in
   `README.md` and `CLAUDE.md` say "newest", so they don't need touching).
3. Update `CLAUDE.md` if the *architecture* changed.
4. Bump `SITE_VERSION` in `assets/data.js` if content or behaviour changed.
5. Commit and push — pushing `master` publishes. Check `git remote -v` says
   `china-2026` first.
