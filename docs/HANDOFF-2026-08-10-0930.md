# Handoff — start here in a fresh session

**Last updated: 2026-08-10 09:30.** Read this, then `CLAUDE.md` for the
architecture. There is only ever **one** handoff file, timestamped in its
filename — `git mv` it to the new timestamp when you update it; if you find
two, the newest wins.

## Where this stands

**v4.2.0 — the ten-feature build, plus flow cards and the 13-pack hanzi syllabus.** v4.0.0 shipped live 2026-08-09; v4.1.0 added cross-linking (home feature cards, story hero jump chips, quiz cross-links, passport explore links); v4.2.0 grew the hanzi playground to 212 characters in 13 themed packs that unlock as you trace (10 per pack opens the next), all stroke data vendored. Paul asked for ten features to make the
site remarkable for both kids and adults, approved the plan
(`~/.claude/plans/do-all-10-create-iterative-treehouse.md`), and all ten are
built and committed, one commit per feature, `node tools/check.mjs` green
(24 checks) after every one. **Nothing is pushed** — Paul reviews before
anything goes live.

What shipped, in commit order: shared `store`/`REDUCED_MOTION` foundations;
`journey.html` + `play.html` shells with the 7-link nav and the `extras.js`
data layer; the sound layer (5 CC0 Freesound loops, SoundKit, off-by-default
toggle); Quiz 2.0 (2-player pass-the-phone + canvas share sticker); the
Journey scrollytelling route map; the dynasty timeline with the
Meanwhile-in-Britain strip; Then & Now sliders (3 PD historics from Commons);
the hanzi playground (vendored Hanzi Writer, 12 characters from the trip
names); Spot-It; the Scale-o-matic; Maisie's Passport; and the hidden-cat
hunt with the Palace Cats bonus + printable certificate.

**Mid-build correction from Paul:** the real itinerary was Shanghai base
first (Xitang day trip), then 3 nights in Xi'an (Warriors day trip), then
Beijing base (Forbidden City + Mutianyu day trip). `JOURNEY` and the journey
page encode this; existing trip copy was audited and doesn't contradict it.
All narrative copy came from the maisie agent (note: her no-exclamation-marks
rule, per `docs/maisie-voice-interview.md`).

## Loose ends

1. **Nothing has been eyeballed in a browser.** check.mjs is structural only.
   The whole v4 surface needs Paul's manual pass (or an approved Playwright
   session — ask first, per his preferences): journey scroll-draw, timeline,
   sliders (keyboard + touch), hanzi trace/speak, Spot-It, comparators, quiz
   2-player + sticker PNG, passport stamps, all 7 cats → bonus → certificate
   print, sound toggle on iPhone (tap-to-resume after navigation). Plus a
   reduced-motion pass and a private-window pass.
2. **Pushing publishes.** When Paul is happy: `git push` (remote is
   `china-2026` — verify with `git remote -v`).
3. Carried over from v3.2.1: 9 unreferenced FC photos (`tools/orphans.mjs`),
   family-moments checklists (`docs/*-moments.md`), Great Wall caption
   identities unconfirmed, Xi'an TASL source URLs lost.
4. The hanzi 🔊 button depends on the device having a zh voice — it hides
   itself otherwise; worth checking on the family's actual phones.

## How to verify

```bash
cd C:\Claude\China
python3 -m http.server 8000    # journey.html, play.html, story.html?trip=…
node tools/check.mjs           # must print "All checks passed." (24 checks)
node tools/orphans.mjs         # advisory; 9 known FC leftovers only
```

## Closing out a session

1. Rewrite this file: current state, what's next, open decisions. **Keep it
   under ~5 KB.** Don't duplicate `CLAUDE.md`, `docs/authoring.md` or memory.
2. `git mv docs/HANDOFF-<old>.md docs/HANDOFF-YYYY-MM-DD-HHMM.md`.
3. Update `CLAUDE.md` if the *architecture* changed.
4. Bump `SITE_VERSION` in `assets/data.js` if content or behaviour changed.
5. Commit and push — pushing `master` publishes. Check `git remote -v` says
   `china-2026` first.
